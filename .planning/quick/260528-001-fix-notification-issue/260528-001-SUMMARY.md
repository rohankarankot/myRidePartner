---
status: complete
---

# Summary: fix the notification issue

## Completed Tasks
- [x] Injected `NotificationsService` in backend `JoinRequestsService`.
- [x] Created `JOIN_REQUEST` database and push notification to notify the trip captain when a passenger requests to join.
- [x] Created `TRIP_UPDATE` database and push notification to notify the passenger when their request is approved or rejected.
- [x] Corrected the mobile client's tap/press notification routing order in `NotifeeService.handlePress` and `NotificationsScreen.handleNotificationPress` so `JOIN_REQUEST` notifications successfully navigate to `/requests/[documentId]` instead of the incorrect `/trip/[id]`.

## Verification Results
- Successfully built the backend app (`npm run build`).
- Checked and passed ESLint checks on all modified files without warnings or errors.
