# Plan: rider pickup notification

## Goal
Ensure a database and push notification of type `TRIP_UPDATE` is sent to the trip captain when a passenger marks themselves as arrived at the pickup point.

## Proposed Changes

### Backend
* [MODIFY] `backend/src/join-requests/join-requests.service.ts`
  - Inside `updatePickupStatus`: If `hasArrived` is true, call `notificationsService.create` to send a `TRIP_UPDATE` notification to the captain (`updatedRequest.trip.creatorId`).
